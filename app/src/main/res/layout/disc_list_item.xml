<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <view
        android:id="@+id/discImageView"
        class="androidx.cardview.widget.CardView"
        android:layout_width="408dp"
        android:layout_height="731dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/discNameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/disc_name"
        app:layout_constraintBottom_toBottomOf="@+id/discImageView"
        app:layout_constraintEnd_toEndOf="@+id/discImageView"
        app:layout_constraintStart_toStartOf="@+id/discImageView"
        app:layout_constraintTop_toTopOf="@+id/discImageView" />
</androidx.constraintlayout.widget.ConstraintLayout>